# requirements-linux.txt
# =======================
# Minimum Python packages required to RUN the built Merbana binary on Linux.
# (The binary is self-contained, but building it requires these as well.)
#
# Install with:
#   pip install -r requirements-linux.txt
#
# Also install the GTK / WebKit system packages (Debian / Ubuntu):
#   sudo apt update && sudo apt install \
#       python3-gi python3-gi-cairo gir1.2-gtk-3.0 \
#       gir1.2-webkit2-4.1
#
# OR for the Qt backend:
#   pip install PyQt5 PyQtWebEngine qtpy

pywebview>=5.0
pyinstaller>=6.0
